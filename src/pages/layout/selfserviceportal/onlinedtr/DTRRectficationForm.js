import { Box,Typography } from '@mui/material';
import React from 'react';

export const DTRRectificationFor = React.forwardRef((props,ref=>{
    return(
        <Box>
            <Typography>PRINT</Typography>
        </Box>
    )
}))