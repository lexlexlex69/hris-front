import { Grid } from '@mui/material';
import React from 'react';
export const DeleteGroupname = ({data}) =>{
    return (
        <Grid container spacing={2}>
            <Grid item xs={12}>
                
            </Grid>
        </Grid>
    )
}