import { Box, Grid, Typography } from "@mui/material";
import React from "react";

export const Lapse = (props)=>{
    return(
        <Box>
            <Grid container>
                <Grid item xs={12}>
                    <Typography>Participants Status</Typography>
                </Grid>
            </Grid>
        </Box>
    )
}