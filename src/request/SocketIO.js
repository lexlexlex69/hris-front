import io from "socket.io-client";

// export const socket = io(window.location.protocol + '//' + window.location.hostname+':3001');
// export const socket = io('http://localhost:3001');
export const socket = null;
export const enabledSocket = false;